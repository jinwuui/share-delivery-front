import 'package:flutter/material.dart';

class PickPlaceOnMap extends StatelessWidget {
  const PickPlaceOnMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(leading: ,),
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
